package com.sebastian.dev.projecttaskmanagement.repository.entities;

//Creating roles in this layer because it only concerns the repository layer
public enum Role {
    PROJECT_USER, ADMIN, PROJECT_MANAGER
}
